# Review-3: gguf-native-serving skill - интеграция fix-1 сессии (2026-09-18)

Review target: `.veai/skills/gguf-native-serving/` - дельта 4 файлов (TRAPS.md, SKILL.md, ORCHESTRATION.md, tasks/task-06). Метод: полный git diff vs HEAD 6e673ab прочитан построчно (179+/5- по 6 файлам, каждый ханк сопоставлен с сессией-автором); git status/log каталога; grep всех счётчиков ловушек; проверка субъекта коммита 5b72aba через git log; сверка каждого числа нового контента с ground truth: `.tasks/fix1-radix-track-seqlen/TASK.md`, `.tasks/fix1-radix-track-seqlen/verification/hardware-report.md`, кампанийные факты (1)-(7), память ft-gate-baseline-nan-flake (числа гейта T49).

## Матрица PASS/FAIL

| Пункт | Критерий | Вердикт |
|---|---|---|
| A | YAML-преамбула SKILL.md байт-в-байт = pre-edit и валидна | **PASS** - git diff SKILL.md vs HEAD содержит единственный ханк `@@ -137,7 +137,7 @@` (строка 140, счётчик "38 ловушек" -> "50 ловушек"); строки 1-9 (преамбула) в диффе отсутствуют => идентичны HEAD; до-редакционное состояние отличалось от HEAD только той же строкой 140 (правка счётчика прошлой сессии по памяти скилла), значит преамбула идентична и pre-edit. Поля: `name: "gguf-native-serving"` = имени папки, kebab-case, 19 симв. (<=50); `schemaVersion: "v0.1"` - точно; description = what+when с триггерами ("Используй, когда пользователь просит поддержку нового gguf формата, нативный gguf-сервинг, или модель сообщает 'architecture X is not supported'"), ~380 симв. (<=1024), `<`/`>` отсутствуют; `agent: Orchestrator` присутствует; `used-by: ["Orchestrator"]` - валидный список, согласован с agent |
| B | Фактическая точность нового контента | **PASS** - противоречий с ground truth 0; каждое число сверено (детали и цитаты в "Доказательствах"): T48 = сигнатура + точный субъект 5b72aba + 65472@4096 / 65536@8191(0.85+mr1); T49 = имя теста, изолированный A/B в обе стороны, 2135 passed / 6 failed; T50 = 1059 vs 1040, ~492 MiB, 0.89 -> 0.90 (2.59 GiB) -> 0.85+mr1; T28+ = 256.00 MiB / ~187 MiB free / driver.py:761 / free-after-init ~4.05 GiB; task-06 expected classes = 65472/65536, ~300@4096 / ~336@8128, chunk-1 warmup ~124, bogus last-full-chunk, decode 13.5-14.7, ladder; ORCHESTRATION §7 = cmdline+start-time, auto-unlink 11->5, IDE helpers (5 sem.mp-*), MEASPID |
| C | Консистентность счётчиков и структуры | **PASS** - grep -rnE "T01-T[0-9]+\|D01-D[0-9]+\|[0-9]+ ловушек" по каталогу даёт ровно 3 вхождения, все согласованы: TRAPS.md:1, SKILL.md:140, ORCHESTRATION.md:282 (все = T01-T50 / D01-D10 / "50 ловушек"); stale "47"/"38"/"T01-T47"/"T01-T38"/"D01-D08" = 0 совпадений. Дубликата do_bench OOM нет: факт расширяет T28 (+3 строки), новой ловушки не создано; T50 -> "см. T28" (TRAPS.md:204), task-06 -> "T28/T50" (:41). Секция "Fix-1 radix кампания 2026-09-18 (scheduler/radix) T48-T50" на TRAPS.md:180, "Debugging" на :208 - размещение корректное (после MMQ-секции, перед Debugging); заголовок "MMQ-prefill кампания 2026-09-17/18 (kernel swap) T39-T47" (:124) не тронут; H2-структура task-06 и ORCHESTRATION сохранена (только добавления) |
| D | Стиль | **PASS** - TRAPS.md новые секции: русский, компактно, em-dash нет, стрелки ASCII "->" (0.89 -> 0.90); task-06 остался английским (консистентно с существующим брифом), новый раздел - только expectation-классы, лабораторного дневника нет (нет дат прогонов, имён логов, PID); ORCHESTRATION.md остался английским; структура заголовков сохранена: все 5 удалённых строки диффа - строки счётчиков и заменённый stale-буллет §8, каждое удаление заменено актуализированной версией того же утверждения, содержательных удалений 0 |
| E | Process hygiene | **PASS** - `git log --oneline -3 -- .` внутри каталога скилла даёт единственный коммит `6e673ab Model implementation skill` - НИЧЕГО не закоммичено; `git status --porcelain` по каталогу: 6 tracked-modified, 0 untracked - README.md не создан, новых файлов нет. Распределение дельты: task-04 (+15: T39-T42) и task-07 (+4: T44/T45) - чисто MMQ-контент прошлой сессии, ни одного упоминания fix-1 (5b72aba/radix/slot-floor/MEASPID/префилл-OOM) => автор их не трогал; §8 ORCHESTRATION (staging hygiene, "committed (6e673ab)") - тоже пред-существующая дельта прошлой сессии (дословно совпадает с описанием в памяти скилла); НОВЫЕ правки строго: TRAPS.md (H1, T28+3, Fix-1-секция), SKILL.md:140, ORCHESTRATION §7 (+10) + §10 счётчик, task-06 (+41) |
| F | SKILL.md полный чеклист | **PASS** - 147 строк (<400); императив ("Реализуй", "СПРОСИ", "проверь", "См."); явные ветвления (phase 7 опциональна, skip-ветка offload-only - единственное санкционированное исключение; upstream waive в task-00); недостижимых шагов нет (каждая фаза привязана к брифу, счётчик "discovery + 7" един); абсолютных локальных путей нет (только относительные: `python/freetoken/models/<family>/`, `models/gguf/config.py`, `build/CMakeCache.txt`); секретов/PII нет; приёмка: "Приёмка кампании" (SKILL.md:142-147), acceptance criteria в брифах (task-06:93-96 проверен), output-контракты в ORCHESTRATION §6 |
| G | Кратность / без дублей | **PASS (1 замечание)** - в TRAPS.md каждый новый факт в одном авторитетном месте: лестница только в T50 (с кросс-рефом на T28), OOM только в T28; task-06 использует кросс-рефы вместо повторов ("post-fix expectations above" в T48-строке :79, "T28/T50" :41, "T43" :38); филлера нет. Замечание-1 (не блокер): лестница 0.89 -> 0.90 -> 0.85+mr1 продублирована дважды внутри task-06 (:40-41 expected-classes и :83-84 T50-ловушка) |

## Блокирующие проблемы

**Не найдено.** Все пункты A-G = PASS. Противоречий с ground truth (TASK.md, hardware-report.md, факты (1)-(7)) не обнаружено; скрытых удалений нет (весь дифф = добавления + 5 строк счётчиков/stale-буллет §8, заменённых актуализированными версиями).

## Замечания (не блокеры)

1. **task-06:35-36** - "(T48)" как ссылка на deep-node LRU eviction gap: T48 не покрывает LRU eviction (T48 - ловушка дропа mamba_last_track_seqlen; LRU-гэп задокументирован в TASK.md out-of-scope, hybrid_radix_cache.py:168-237, собственного номера в TRAPS.md не имеет). Кросс-реф ведёт не туда. Рекомендация: убрать "(T48)" или заменить ссылкой на out-of-scope запись TASK.md.
2. **task-06:37** - "chunk-1 triton warmup ~124 tok/s" без квалификатора чанка: 123.64 измерено на 8128-чанках (hardware-report §8); на 4096-прогоне chunk 1 = 91.86 (§4). Класс ("warmup далеко ниже медианы") верен, число частное для 8128.
3. **ORCHESTRATION.md:220** - "silently breaks the watchdog": ошибка 'MEASPID: unbound variable' фактически попадала в лог (hardware-report §6.3); по существу верно (FAILPAT-мониторинг ломается, EXIT-trap cleanup выполняется - это в тексте и сказано), формулировка чуть категоричнее артефакта.
4. **G/замечание-1** - дубль лестницы внутри task-06 (см. матрицу).

## Доказательства

### Сверка фактов (цитаты)

- `git log --format='%h %s' -1 5b72aba` -> `5b72aba fix(scheduler): carry mamba_last_track_seqlen across prefill chunk transitions` == цитата TRAPS.md:183-184 `фикс 5b72aba "fix(scheduler): carry mamba_last_track_seqlen across prefill chunk transitions"` - точна.
- T48 пост-фикс ожидания (TRAPS.md:193-194): `Пост-фикс, повтор 65k: #cached-token 65472 @4096 / 65536 @8191 (8128-чанки, 0.85+mr1)` == hardware-report §2 (`#cached-token: 65472`, Run A @0.90, chunk 4096) и §8 (`#cached-token: 65536`, Run B retry @0.85+mr1, 8191) == факт (1). Совпадение символ в символ.
- T48 сигнатура (TRAPS.md:191-193): `повторный HIT на крупных чанках (8128 -> 65536), MISS на мелких (4096 -> 0/49) при любом ratio/mr - конфиг-независимый баг (НЕ dtype/mr/ratio/timing), зависит только от chunk size` == TASK.md `repeat = FULL MISS (0/49 cached) at ratio 0.85/0.89/0.90, mr4 and mr1. Config-independent` + `fp8 KV dtype is NOT the cause; nor mr, nor ratio, nor request timing (60 s gaps)`.
- T49 (TRAPS.md:195-200): `прецедент test_gguf_expert_banks.py::test_grid_caps_raise_before_kernel_launch: NaN isfinite только под full-suite порядком; гейт 2135 passed / 6 baseline-class failed` и `проходит изолированно И с диффом, И без` == память ft-gate-baseline-nan-flake: `2135 passed / 6 failed / 206 skipped`, `Passes in isolation both with the wave's prefill.py fix reverted to HEAD and with the fix in place -> Environment class` == факт (6).
- T50 (TRAPS.md:201-206): `~492 MiB hoptodesk-налог` == TASK.md `hoptodesk cost ~492 MiB on 2026-09-17` == факт (2); `"minimum of 1059 layer-0-width slots vs planned 1040" при --memory-ratio 0.89` == hardware-report §1 `byte-weighted minimum of 1059 layer-0-width slots vs the planned 1040` (engine/cache_budget.py:77); `0.89 -> 0.90 (free-after-init 2.59 GiB)` == §1 Run A (final) `free-after-init 2.59 GiB`; `для 8191 нужен 0.85+mr1 (см. T28)` == §8.
- T28 extension (TRAPS.md:74-76): `8191@0.90 OOM в ПЕРВОМ префилл-чанке (triton do_bench, driver.py:761: 256.00 MiB при ~187 MiB free) - для 8191 нужен 0.85+mr1 (free-after-init ~4.05 GiB; измерено 2026-09-18)` == §4 `torch.OutOfMemoryError: CUDA out of memory. Tried to allocate 256.00 MiB. GPU 0 ... 187.00 MiB is free in triton/backends/nvidia/driver.py:761` + §8 `free-after-init 4.05 GiB` == факт (2).
- task-06:33-41 expected classes: `#cached-token 65472 @4096-chunks / 65536 @8191-chunks` ✓ (§2/§8); `~300 @4096 / ~336 @8128 tok/s class` == §4 `MEDIAN 300.58` и §8 `Median ... = 336.43` (факт (3)); `chunk-1 triton warmup ~124 tok/s` == §8 chunk 1 = 123.64; `bogus last-full-chunk line, T43` - число в task-06 не цитируется, T43 держит кампанийный класс ~1552-1602, fix-1 прогон дал 1748.80, §8 сам называет его `same class as the documented ~1552-1602` - противоречия нет (факт (3)); `Decode steady: 13.5-14.7 tok/s class` == §4/§8 + факт (3); `Boot fallback ladder ...: 0.89 -> 0.90 -> 0.85+mr1 for 8191 (8191@0.90 OOMs in the first-chunk triton do_bench, T28/T50)` == факты (2),(3).
- ORCHESTRATION §7 новые буллеты (:211-220): `SWEEP verification: before killing leftovers verify cmdline + start-time (spawn_main children reparented to systemd = stale orphans)` == факт (5) и §5 отчёта (spawn_main-сироты PID 776127/2010750); `IDE helpers legitimately hold baseline semaphores (a PyCharm forkserver held 5 live sem.mp-*) - attribute census deltas BEFORE killing, never kill IDE processes` - число 5 согласовано арифметикой §5/§8 TASK.md (итог 11; 6 держали 2 задокументированных сироты; после реапа 11 -> 5, остаток - forkserver); `sem.mp-* files auto-unlink when the holding multiprocessing resource_tracker dies (observed 11 -> 5, zero manual rm)` == факт (5) и TASK.md follow-up `semaphores 11 -> 5, 2 stale PIDs reaped` (реап PID -> смерть resource_tracker -> авто-unlink; "zero manual rm" не противоречит реапу процессов); `a PID variable the watchdog references (MEASPID) must be exported BEFORE the watchdog block - an unset MEASPID under 'set -u' silently breaks the watchdog (the EXIT-trap cleanup still ran)` == факт (4) и §6.3 отчёта (`MEASPID: unbound variable`; `the pkill of the ft tree still executed`).

### Гигиена процесса

- `git rev-parse --show-toplevel` = /media/ai/src/FreeToken; `git log --oneline -3 -- .` в каталоге скилла -> только `6e673ab Model implementation skill`: после 6e673ab ничего не закоммичено.
- `git status --porcelain` по каталогу скилла: ровно 6 M-записей (ORCHESTRATION.md, SKILL.md, TRAPS.md, tasks/task-04-kernel-dispatch.md, tasks/task-06-e2e-ab.md, tasks/task-07-cpu-compute-tier.md), untracked (`??`) в каталоге скилла нет - README.md не создан, новых файлов нет; дельта 179+/5- остаётся незакоммиченной (требование выполнено).
- Построчное распределение диффа: SKILL.md = 1 ханк (счётчик :140); TRAPS.md = H1 (:1), T28 +3 (:74-76), ханк @@ -118,6 +121,90 @@ = MMQ-секция прошлой сессии + Fix-1-секция новой (+84), D09/D10 (+11); ORCHESTRATION.md = §7 +10 (новая), §8 +8/-2 (прошлая), §10 счётчик (новая); task-06 = +12 (Expected result classes :31-41) +29 (ловушки :63-91); task-04 = +15 (T39-T42), task-07 = +4 (T44/T45) - MMQ-контент прошлой сессии, fix-1-упоминаний 0.

### Счётчики (grep, полный вывод)

```
ORCHESTRATION.md:282:- [TRAPS.md](TRAPS.md) - T01-T50 + recipes D01-D10; the pre-close checklist
SKILL.md:140:См. [TRAPS.md](TRAPS.md) - 50 ловушек, каждая из которых стоила реального
TRAPS.md:1:# TRAPS.md - 50 ловушек для native GGUF serving (T01-T50, рецепты D01-D10)
```

### Пример цитат нового контента

TRAPS.md:186-194 (T48, дословно): `- **T48** (scheduler, fixed 5b72aba) Дроп поля на переходе префилл-чанков: mamba_last_track_seqlen (L) не форвардился из pending_req.chunked_req в continuation Req (try_add_one, scheduler/prefill.py) -> промпт, чей ПОСЛЕДНИЙ префилл-чанк кончается ниже x64 track boundary, финиширует с L=None -> нет GDN snapshot donation -> МОЛЧИВЫЙ radix MISS на идентичном повторе.` - соответствует TASK.md root cause (try_add_one ~236-250 не форвардил поле; core.py:52 default None; page-64 alignment).

## Итоговый blocking_status: pass
