# Review-1: .veai/skills/merge-main-into-vektory79/SKILL.md

- Объект: `.veai/skills/merge-main-into-vektory79/SKILL.md` (185 строк).
- Референс конвенций: `.veai/skills/code-with-subagents/SKILL.md` (889 строк; agent: "Orchestrator").
- Режим: review-1 черновика перед доработкой; файл навыка не редактировался.
- Метод: полное чтение файла + референса, листинг папки навыка, программная проверка YAML-метрик (длина description/name, символы `<`/`>`, наличие used-by, пин номеров строк маркеров), сверка покрытия опыта с Memory Bank (vektory79-main-rewrite-gotcha, git-integration-preferences, ft-pytest-worktree-baseline-gotchas, process-hygiene-protocol, lean-subagent-context, ft-serve-test-and-e2e-gotchas, ft-gguf-nvfp4-1m-capacity-frozen-shim, glm53-flash-nvfp4-cache-budget).
- Папка навыка: только SKILL.md (README.md и вспомогательные файлы отсутствуют) - PASS.

## Матрица PASS/FAIL

### 1. YAML-преамбула - PASS
| Подпункт | Вердикт | Доказательство |
|---|---|---|
| Открывается `---` и закрывается `---` | PASS | строки 1 и 6 |
| name == имени папки, <=50 символов, kebab-case без пробелов/заглавных | PASS | строка 2: `name: "merge-main-into-vektory79"`; замер: 25 символов; совпадает с папкой `.veai/skills/merge-main-into-vektory79/` |
| schemaVersion ровно "v0.1" | PASS | строка 3: `schemaVersion: "v0.1"` (та же форма, что в code-with-subagents:3) |
| description: что делает + когда использовать, <=1024 символов, без `<` и `>` | PASS | строка 4; замер: ровно 436 символов (<1024); описывает действие (обновление vektory79 мерджем origin/main по регламенту) и когда использовать (3 триггерные фразы + сигнал о новых коммитах в origin/main или origin/master); программная проверка HAS_LT_GT = False |
| agent присутствует и валиден | PASS | строка 5: `agent: "Orchestrator"` - валидный executor (code-with-subagents:5) |
| used-by отсутствует (режим "все агенты") | PASS | подстрока `used-by` в файле: 0 совпадений |

### 2. Ясность и детерминизм - PASS (замечания W-1..W-4 не рушат пункт)
| Подпункт | Вердикт | Доказательство |
|---|---|---|
| Шаги императивные и однозначные | PASS | "Прочитай"/"Создай" (54-56), "Резолв"/"Собрать" (65-67), нумерованные шаги Wave 2 (92-132) |
| Решения через явные if/else | PASS | ESCALATE vs DIRECT_EXECUTION (82-84); "не ff - СТОП и отчёт" (95); boot-smoke обязателен/опционален по триггеру (110-111); падение smoke по причине мерджа vs неочевидная причина (126-128); TP/FP/Need more data (148); NEEDS-USER-DECISION при обоих refs (44-45); стоп при другой текущей ветке (46) |
| Триггерные фразы в description реалистичны | PASS | "примерджить origin/main в мою ветку", "обновить ветку main-ом", "смержить новые коммиты main" (строка 4) |
| Входные данные перечислены явно | PASS | раздел "## Входные данные (разреши до старта)" (41-48): целевой ref, ветка, task-каталог, подтверждение правил |
| Формат вывода указан | PASS | "## Формат вывода" (161), 8 нумерованных пунктов (165-172) |
| Приёмочный чек-лист присутствует | PASS | "## Приёмочный чек-лист" (174), 10 пунктов (176-185) |

### 3. Субагенты - FAIL
| Подпункт | Вердикт | Доказательство |
|---|---|---|
| Точные инструменты названы | PASS | `call_code_agent` (30, 58, 86, 146), `call_review_agent` (31, 134), `call_test_agent` (32) |
| Формат результата каждой роли (одна форма на вызов) | **FAIL** | у `call_test_agent` (32) ожидаемый формат результата не указан нигде в файле. У остальных вызовов задан ровно один формат: discovery - "компактная сводка в report_result + полный отчёт в `<task>/discovery-report.md`" (61-62) = резюме+артефакт; merge-волна - "merge-commit hash, решения конфликтов, счётчики тестов, smoke-вердикт, инварианты + артефакты" (88-89); review - "нумерованные findings [file:line] severity + evidence + явные вердикты" (141-142) = встроенное резюме; triage - "Каждая finding -> TP / FP / Need more data с обоснованием" (148) = встроенное резюме |
| Стратегия разбиения задач субагентов (по волнам/ролям; параллельность только где безопасно) | PASS | "Разбиение задачи: одна роль, один агент" (60); параллельны только 2 read-only review-прохода A/B (139-140); Wave 4 - resume автора merge-волны (146) |
| Лимит циклов <=4 или обоснован | PASS | "не более 3 циклов на логическую волну" (38-39); "Не сошлись за 3 цикла - стоп, отчёт пользователю" (150) |

### 4. Сложность и циклы - PASS
- Недостижимых шагов нет: прослежено Wave 0 (52) -> Wave 1 (58) -> Выбор режима (80) -> Wave 2 (86) -> Wave 3 (134) -> Wave 4 (146) -> Wave 5 (152).
- Ветвления сходятся или завершаются: ESCALATE (83) и NEEDS-USER-DECISION (45) завершаются у пользователя; SKIP-ветка review (136-138) корректно ведёт в Wave 4 с 0 findings и далее в STOP GATE; 6d имеет обе исходящие ветки: fix + повторный smoke либо СТОП (126-128).
- Циклы: quality-цикл <=3 (38-39, 150) - в лимите <=4.

### 5. Краткость и безопасность - PASS
| Подпункт | Вердикт | Доказательство |
|---|---|---|
| Файл <400 строк | PASS | 185 строк (замер) |
| Нет заполняющей прозы | PASS | файл плотный, без водянистых абзацев |
| Нет разрушительных действий без явного подтверждения | PASS | запреты перечислены (15-17: push, gh, rebase, reset --hard, force-push, удаление веток); backup-ветка до merge (93); "чужие живые процессы НЕ убивать" (106); kill -9 только своего зависшего дерева (121-122). Замечание W-2 - см. ниже |
| Нет абсолютных локальных путей | PASS | `/media/` и `/home/` в файле: 0 совпадений; плейсхолдеры `<репозиторий>` (112), `<task>` (62), `<short-sha>` (93), `<old-HEAD>`/`<цель>` (130); порт 18081 - project-конвенция (111) |
| Секретов нет | PASS | токенов/ключей в файле нет |
| README.md в папке навыка не создавался | PASS | листинг `.veai/skills/merge-main-into-vektory79/`: только SKILL.md |

### 6. Покрытие опыта - 17/17 COVERED (существенность сохранена)
| # | Пункт | Статус | Доказательство (строки SKILL.md) |
|---|---|---|---|
| 1 | правило дубликатов main-wins | COVERED | 22-24, 98-101; чек-лист 178 |
| 2 | ровно один merge-коммит + amend | COVERED | 19-21, 129-131 (rev-list count == 1, parents, amend); чек-лист 177 |
| 3 | .veai исключены | COVERED | 25, 131 (".veai в коммите 0"); чек-лист 183 |
| 4 | тесты до финализации | COVERED | 26, 102-109 ("Верификация ДО финализации"); чек-лист 180 |
| 5 | ff-only для main | COVERED | 95 (`git fetch origin main:main`; не ff - СТОП) |
| 6 | backup-ветка | COVERED | 93 (`backup/vektory79-pre-mergeN-<short-sha>`) |
| 7 | pathspec-stash | COVERED | 92, 132 |
| 8 | process-hygiene (census/FAILPAT/сериализованность/SIGTERM->SIGKILL) | COVERED | 36-37; 104-106 (pgrep + /dev/shm sem census, чужие не убивать); 115-117 (trap-on-EXIT runner, FAILPAT, timeout 900s); чек-лист 182 |
| 9 | python -m pytest import-режим | COVERED | 108-109 (с объяснением причины ложных ModuleNotFoundError) |
| 10 | uv run --extra dev | COVERED | 108 |
| 11 | boot-smoke триггер по зонам риска (frozen-shim урок) | COVERED | 70-74 (зоны (a)-(d); (a) прямо называет frozen GgufConfigShim и "краш ВСЕХ gguf-бутов"); 110-112 |
| 12 | envelope-зависимость boot-чисел | COVERED | 123-125 (инварианты вместо точных чисел); чек-лист 181 |
| 13 | backend-death hang recovery | COVERED | 116 (FAILPAT-часовой), 121-122 (kill -9 дерева при "Waiting for connections to close") |
| 14 | STOP GATE | COVERED | 154-157 |
| 15 | лимит quality-циклов | COVERED | 38-39, 150 |
| 16 | lean-context для субагентов | COVERED | 33-35 (включая правило resume_id) |
| 17 | резолв origin/master vs origin/main | COVERED | 43-45 (включая NEEDS-USER-DECISION при обоих refs), 65-66; description (строка 4); чек-лист 176 |

Проверка "должен содержать логику code-with-subagents" (по существенности, сжатие осознанное): DIRECT/ESCALATE (82-84), волны Code/Review/Triage + условный Test (29-33), Review x2 с фокусами A correctness / B edge (139-142), triage TP/FP (148), STOP GATE (154-157), handoff "Передай: путь discovery-отчёта, правила ограничений, эталонные boot-числа" (89-90), lean-context (33-35) - присутствуют. Сжатие до 185 строк не теряет существенного.

## Блокирующие проблемы

- **B-1** - Type: Test, Severity: Error. `SKILL.md:32` - у роли `call_test_agent` не задан ожидаемый формат результата (одна из форм: краткое резюме + путь к артефакту / встроенное резюме / резюме без артефакта). Чек-лист требует: "каждая роль субагента имеет ожидаемый формат результата; каждый вызов указывает одну форму". Для трёх других вызовов формат задан, для test-роли - нет (оркестратор не сможет провалидировать её результат). Минимальный фикс: одна строка рядом со строкой 32 или в месте вызова, например: "Ожидаемый результат call_test_agent: краткое резюме + путь к отчёту о прогоне в task-каталоге" (ровно одна форма).

## Неблокирующие замечания

- **W-1** (Warning, Design): `SKILL.md:110` - битая ссылка "из Wave 1.5 (a)-(d)": волны 1.5 в файле нет; зоны риска определены в Wave 1, п.5 (строки 70-74). Заменить на "из Wave 1, п.5 (a)-(d)".
- **W-2** (Warning, Design): `SKILL.md:96-97` - "авто-закмиченный merge откатать и повторить с --no-commit" не задаёт разрешённый механизм отката, при том что строки 15-16 запрещают `git reset --hard` на пользовательских ветках. Указать явный разрешённый путь (например `git merge --abort` до коммита; либо явно авторизовать узкий `git reset --hard <old-HEAD>` при чистом дереве и созданном backup).
- **W-3** (Warning, Bug-risk): `SKILL.md:104-109` - у целевых тестов (6b) нет ветки на НОВЫЕ падения: явная ветка есть только у boot-smoke (126-128), а триаж падений отложен до STOP GATE (154-155), т.е. после финализации merge-коммита. Рекомендация: добавить "новые падения не из известного baseline и не Environment -> СТОП/triage ДО финализации (фикс - amend, как в 6d)".
- **W-4** (WeakWarning): `SKILL.md:93` - preflight требует "stash пуст", но действие при непустом stash не определено (имплицитный STOP). Сформулировать явно.
- **T-1** (Typo): `SKILL.md:155` - "Environment-контенция волн" -> "конкуренция волн (Environment)".
- **T-2** (Typo): `SKILL.md:69` - "(или только старый слитый дельта)" - нечёткая фраза; предложить "(или тронут только старым уже слитым дельта-коммитом)".
- **I-1** (Info): `SKILL.md:117-118` ("boot-числа совпадают с эталоном в безопасную сторону") vs 123-125 ("а не точные числа") - лёгкая внутренняя напряжённость формулировок; оперативные инварианты заданы корректно (123-125), первую фразу лучше свести к списку инвариантов.
- **I-2** (Info): параметры "профиль fast/strong" (58, 86) в code-with-subagents не встречаются - убедиться, что текущая схема call_code_agent их поддерживает.
- **I-3** (Info): census `pgrep -af 'pytest|ft serve|benchbw|ft_run'` (105) матчит собственную shell-обёртку census (известная безвредная особенность из merge-сессии) - полезна оговорка "фильтровать сам матч census перед флагованием остатков".

## Доказательства

Преамбула (строки 1-6) - PASS по всем YAML-подпунктам:
```
---
name: "merge-main-into-vektory79"
schemaVersion: "v0.1"
description: "Аркестрирующий навык: обновляет ветку vektory79 мерджем в неё origin/main по регламенту этого репозитория. Включает оркестрацию субагентами (Code/Review/Triage), process-hygiene тестовых волн, GGUF boot-smoke и правила разрешения конфликтов. Используй, когда пользователь просит 'примерджить origin/main в мою ветку', 'обновить ветку main-ом', 'смержить новые коммиты main' или сообщает о новых коммитах в origin/main или origin/master."
agent: "Orchestrator"
---
```

B-1 (строка 32) - единственное упоминание call_test_agent, без формата результата:
```
- `call_test_agent` - только если нужно расширить покрытие (обычно не нужно: тесты гоняет merge-волна).
```
Для сравнения, формат результата задан у остальных вызовов (61-62, 88-90, 141-142, 148):
```
Ожидаемый результат: компактная сводка в report_result + полный отчёт в
`<task>/discovery-report.md`. Собрать:
```
```
Ожидаемый результат: merge-commit hash, решения конфликтов, счётчики тестов, smoke-вердикт,
инварианты + артефакты. Передай: путь discovery-отчёта, правила ограничений, эталонные
boot-числа из памяти (если есть). Шаги агента:
```
```
инварианты, тест-гэпы, доки-vs-код). Ожидаемый результат каждого: нумерованные findings
[file:line] severity + evidence + явные вердикты по каждому фокусу.
```
```
1. Каждая finding -> TP / FP / Need more data с обоснованием из текущего кода.
```

W-1 (строка 110) - битая ссылка на несуществующую волну:
```
   c. BOOT-SMOKE, триггер: merge затронул ЛЮБУЮ зону риска из Wave 1.5 (a)-(d) - тогда
      обязателен; иначе опционален. Рецепт: port 18081 (18080 занят wrapper-ом пользователя),
```
(зоны (a)-(d) определены в Wave 1, п.5, строки 70-74; "Wave 1.5" в файле не существует)

W-2 (строки 96-97) - откат без заданного механизма, при запрете reset --hard (15-16):
```
4. `GIT_EDITOR=true git merge main --no-commit` (авто-закмиченный merge откатать и повторить
   с --no-commit, если git закоммитил сам).
```
```
  `git rebase`, `git reset --hard` на пользовательских ветках, переписывание истории,
  force-push, удаление веток пользователя.
```

W-3 (строки 102-109) - ветки падения целевых тестов нет (явная ветка только у boot-smoke, 126-128):
```
6. Верификация ДО финализации:
   a. Conflict-marker grep по python/ tests/ docs/ - пусто.
   b. Целевые тесты (сериализованно, каждый под timeout, pre/postflight census
      `pgrep -af 'pytest|ft serve|benchbw|ft_run'` + `ls /dev/shm | grep -c "^sem\.mp-"`,
      чужие живые процессы НЕ убивать): пиновые тесты ветки (gguf-banks, glm5_next,
      qwen3_vl, qsa-набор, tests/engine), новые тесты из main-коммитов, import-smoke.
      Запуск строго `uv run --extra dev python -m pytest` (не консольный pytest - import-режим
      глотает repo-root из sys.path и даёт ложные ModuleNotFoundError).
```

T-1 (строка 155) - опечатка:
```
baseline / Environment-контенция волн / исправлено); review выполнен или обоснованно skipped;
```

Выборочные цитаты покрытия (PASS):
```
- Лимит quality-циклов: Review x2 -> Triage -> Fix TP - не более 3 циклов на логическую
  волну; не сошлись - стоп и отчёт пользователю (что сделано, TP/FP вердикты, варианты).
```
(38-39)
```
      Boot-числа (слоты/KV) ЗАВИСЯТ от стартовой свободной VRAM - сравнивай инварианты:
      слот-гейт молчит, суммарные слоты >= рабочего набора, "больше свободно = безопаснее",
      а не точные числа.
```
(123-125)
```
      Если backend умер, а uvicorn висит в "Waiting for connections to close" - kill -9
      дерева принудительно (graceful не завершится).
```
(121-122)
```
- Lean-контекст (правило затрат): в `context` субагента - только точные пути артефактов,
  file:line якоря и решения; содержимое файлов субагент читает сам по пути. Resume
  (`resume_id`) - только когда субагент уже держит нужные файлы и дельта задачи мала.
```
(33-35)
```
1. Preflight: статус чист (грязь только .veai/* - pathspec-stash их и pop после финализации),
   stash пуст, нет незавершённых операций. Backup: `git branch backup/vektory79-pre-mergeN-<short-sha>`.
```
(92-93)

## Итоговый blocking_status: fail

Единственная блокирующая проблема - B-1 (у `call_test_agent`, SKILL.md:32, нет ожидаемого формата результата). Все остальные пункты чек-листа PASS; W-1..W-4, T-1, T-2, I-1..I-3 - неблокирующие доработки для fix-волны.
