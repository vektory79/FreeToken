# Findings triage: reviews of merge commit 380e9eb (vektory79)

Reviews triaged: Review-A (A1-A4), Review-B (B1-B4 + gaps G1-G6).
Method: every verdict is grounded in the CURRENT tree at 380e9eb (code/docs read
directly, not from reviewer wording); parent state cross-checked with
`git show 6e673ab:...` / `git show afd99cb:...` where provenance matters.
Verdicts: TP (confirmed), FP (not confirmed), INFO (operational, no code change).
Fixes land in code/docs only; test gaps (G1-G6) are follow-up, intentionally not
fixed in this merge.

| # | Finding | Verdict | Grounding (current tree) | Action |
|---|---------|---------|--------------------------|--------|
| A1 | docs/models.md fp8 bullet truncated to a dangling first line | TP (truncation confirmed; completion belongs in cli.md, not models.md) | models.md:60 (last line) ends mid-sentence on a comma; branch 6e673ab:docs/models.md lines 45-50 carried the full bullet (plain paged + hybrid-SWA + QSA pools, model list, QSA 16-bit index-key caveat, MLA/DSA/DSV4/MiniMax-M3 rejection). The merge kept only its first line. docs/cli.md #fp8-kv-cache already documents every unique fact; the branch's model-name enumeration duplicates models.md's per-architecture content, so per the user's duplicate rule main's structure wins and nothing is copied over | Dangling line deleted from models.md (no dangling text remains); cli.md FP8 section corrected to factual behavior (see A2) |
| A2 | args.py help + cli.md FP8 bullet say fp8 is "not MLA/DSA ... fails at startup", but the merged code accepts fp8 on MLA/DSA | TP | engine.py:1919-1941: `kv_quant != "none"` accepted for required types {FULL, SWA, QSA, MLA, DSA}; kvcache/__init__.py:280-295 passes kv_quant into DSAKVCache / KpoolDSAKVCache / MLAKVCache; dsa_pool.py:71 `if self.kv_quant in ("fp8", "nvfp4")` + :143 fp8 path; attention/__init__.py: `dsa` backend declares supports_fp8_kv=True (so do triton and qsa_sparse). Rejected only: DSV4 paged pool and BSA (`_reject_unsupported_quant`). Provenance: inherited from the branch - 6e673ab:engine.py:1811/1825 already had the same accepting type set while 6e673ab:args.py:443 had the same stale help; main (afd99cb) never had this check. Reviewer-B's "merge did not introduce it" confirmed | args.py help rewritten to the factual pool/backend matrix (nvfp4 sentence aligned too: code accepts MLA/DSA for nvfp4, head_dim % 16 == 0); cli.md FP8 bullets updated: scales applied by triton (plain paged / hybrid-SWA), qsa_sparse (QSA), dsa (MLA/DSA); DSA kernel dequantizes selected latent rows, index-key/tail tiers stay 16-bit; DSV4 + block-sparse MiniMax-M3 fail at startup |
| A3 | graph.py duck-typing fix changed behavior: tuple/generator used to materialize via list(...), now wrap as a single element and would break on .reset() | TP | 6e673ab:graph.py:121-125 normalized `[x] if isinstance(x, OffloadMoeCache) else list(x or [])` (any iterable materialized); merged graph.py:133-142 wraps any non-list as ONE element. Production call sites engine.py:523 and :1344 pass `self.moe_offload_caches or self.moe_offload_cache` (list / single / None) - production unaffected. The merged shape exists because the branch's isinstance(OffloadMoeCache) gate broke the duck-typed test double (failure pre-existing on 6e673ab) | B2's recommendation applied: `isinstance(moe_offload_cache, (list, tuple))` -> materialize; None -> []; single (incl. test doubles) -> [x]. Residual divergence: generators still wrap single - no caller passes one (GraphRunner is constructed only at engine.py:523/:1344); documented as accepted |
| B2 | same as A3; recommend isinstance (list, tuple) | TP | same evidence as A3 | applied together with A3 |
| A4 | models/register.py: branch-side hf_quant_config.json sidecar fetch dropped by the auto-merge; main re-implemented it as _merge_sidecar_quantization_config | TP (resolution correct; adaptation-log undercounted, code untouched) | register.py:328 checkpoint_quant_config only calls QuantConfig.from_hf(hf_config, ...) - no sidecar read anywhere in it; utils/hf.py:202-231 _merge_sidecar_quantization_config reads hf_quant_config.json inside _load_hf_config (config.json wins when it has quantization_config). Main's implementation supersedes the branch's fetch - duplicate rule, main wins. adaptation-log claimed "exactly one" duplicate - actually two | adaptation-log duplicates section corrected to two cases; no code change |
| B3 | `uv pip install` inside a worktree replaced the editable freetoken in the main .venv | INFO (operational) | environment hazard of running uv from a worktree, not a code defect; no repo artifact involved | recorded here and in adaptation-log only |
| B4 | 11 of 16 baseline failures would be fixed by adding tests/__init__.py (pytest import mode) | INFO (pre-existing, out of scope) | matches the documented baseline classes (test-results.md: muse_glimmer / dsa_kpool / dsa_pool / glm_dsa "No module named 'tests'"); adding tests/__init__.py changes collection behavior far beyond this merge | not fixed; follow-up candidate |
| G1 | no test covers the HAS_BLOCKS x HAS_KV_SCALE combo in the extend kernels | TP (gap), follow-up | tests/kernels/test_triton_attention.py has k_scale stubs (:75/:731/:809) and block_ends tests, but none exercises both flags set together | listed as follow-up |
| G2 | decode-path guard NotImplementedError untested | TP (gap), follow-up | attention/triton.py:227 raises NotImplementedError("bidirectional multimodal blocks need the extend kernel path"); no test hits it | listed as follow-up |
| G3 | GraphRunner never tested with a list of caches | TP (gap), follow-up | tests/moe/test_offload.py:510-523 constructs GraphRunner with a single FakeOffloadCache only | listed as follow-up |
| G4 | GraphRunner never tested with mrope=True | TP (gap), follow-up | same test passes no mrope argument (defaults False) | listed as follow-up |
| G5 | QSA pool mrope path test gap | TP (gap, needs scoping), follow-up | tests/kvcache/test_qsa_pool.py / test_qsa_pool_fp8.py do reference mrope (slab sizing); the specific hole claimed by review B (mrope interplay beyond that) needs a dedicated follow-up test pass | listed as follow-up |
| G6 | moe-guard unified_memory=True branch untested | TP (gap), follow-up | tests/moe/test_offload.py:425 monkeypatches _is_unified_memory_gpu -> False only; the True branch (auto -> fused on unified-memory GPUs) has no test | listed as follow-up |

Extra observation (not a reviewer finding): the branch's "Multimodal checkpoints are
served text-only." line was also dropped by the merge. Correct per main: main's
image-input wave serves image input, so the statement was obsolete. Already covered
by the adaptation-log duplicates section (case 1).
